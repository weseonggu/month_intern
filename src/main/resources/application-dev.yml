
spring:
  application:
    name: AuthService
  data:
    redis:
      host: localhost
      port: 6379
      username: default
      password: systempass
      timeout: 3000
  datasource:
    url: jdbc:mysql://localhost:3306/work
    username: intern
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
    jpa:
      hibernate:
        ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          format_sql: true